How to encode path in each packet?
1. Add path field in PacketSink
2. Populate each element of route vector with path


Which queue to take?
-> Egress queue - Tor_Core and Core_Tor


Tables to update at leaf TOR?
-> Both To and From tables


ECMP: hash_fn (src || dst ) instead of rand() - deterministic